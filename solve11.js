// Добавление и удаление свойств: Создайте объект, добавьте в него свойства и затем удалите одно из свойств.


let person ={
    name: 'danya'
}

console.log(person.name);

delete person.name;

console.log(person.name);
console.log(person);